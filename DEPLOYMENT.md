# 배포 가이드 및 현재 상태

## ⚠️ 현재 코드의 배포 문제점

### 1. BackgroundTasks의 한계
- **문제**: FastAPI의 `BackgroundTasks`는 같은 프로세스 내에서만 작동
- **영향**: 
  - 서버 재시작 시 진행 중인 작업이 사라짐
  - 여러 인스턴스가 있으면 작업이 분산되지 않음
  - HTTP 요청 타임아웃 시 작업이 중단될 수 있음

### 2. 메모리 저장소의 한계
- **문제**: `MemoryStore`는 프로세스 메모리에만 저장
- **영향**:
  - 서버 재시작 시 모든 데이터 사라짐
  - 여러 인스턴스 간 데이터 공유 불가
  - 로드 밸런싱 시 문제 발생

### 3. 영상 분석 시간
- **문제**: 영상 분석은 수분~수십 분 소요
- **영향**:
  - HTTP 요청 타임아웃 (보통 30초~5분)
  - Render/Railway 무료 티어는 제한적

## 🚀 배포 가능한 옵션

### 옵션 1: 최소한의 배포 (단일 인스턴스, 짧은 영상만)

**장점**: 빠르게 배포 가능, 추가 인프라 불필요
**단점**: 제한적, 프로덕션에 부적합

**요구사항**:
- 단일 인스턴스만 사용
- 짧은 영상만 처리 (1-2분 이하)
- 서버 재시작 시 데이터 손실 감수

**적용 방법**:
- 현재 코드 그대로 사용 가능
- Render/Railway에서 단일 인스턴스로 배포

### 옵션 2: 실제 프로덕션 배포 (권장)

**요구사항**:
- PostgreSQL (데이터 영구 저장)
- Redis + Celery (작업 큐)
- 여러 인스턴스 지원

**구현 필요**:
1. PostgreSQL로 데이터베이스 교체
2. Celery + Redis로 작업 큐 구현
3. 환경 변수 설정

### 옵션 3: 서버리스 함수 (Vercel Functions, AWS Lambda)

**장점**: 자동 스케일링, 비용 효율적
**단점**: 실행 시간 제한, MediaPipe 설치 복잡

## 📋 현재 상태로 배포 가능 여부

### ✅ 배포 가능 (제한적)
- **조건**: 단일 인스턴스, 짧은 영상만, 데이터 손실 감수
- **플랫폼**: Render, Railway (단일 인스턴스)
- **사용 사례**: 데모, 프로토타입, 개인 프로젝트

### ❌ 프로덕션 배포 불가
- 여러 사용자 동시 처리
- 긴 영상 분석
- 데이터 영구 저장 필요
- 안정성 중요

## 🔧 배포 가능하도록 개선하기

### 빠른 개선 (옵션 1 유지)
현재 코드로도 단일 인스턴스 배포는 가능하지만, 다음을 추가 권장:
- 타임아웃 처리
- 에러 핸들링 강화
- 로깅 추가

### 완전한 개선 (옵션 2)
1. PostgreSQL 통합
2. Celery 작업 큐 구현
3. Redis 연결
4. 환경 변수 설정

## 💡 추천 방안

**개인 프로젝트/데모**: 현재 코드 그대로 배포 가능 (제한적)
**실제 서비스**: 옵션 2로 개선 필요
